import controller from './controller';
import template from './template.html';
import './style.scss';

/**
 * Just export the confirmation-modal's controller and template since this will be bound to the
 * modal which will be created in the home module.
 */
export { controller, template };
