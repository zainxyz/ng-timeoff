import datePickerTemplate from './datepicker.template.html';
import datePickerDayTemplate from './datepicker.day.template.html';
import datePickerMonthTemplate from './datepicker.month.template.html';
import datePickerYearTemplate from './datepicker.year.template.html';

/**
 * These are some template overrides for the date-picker module to update the styles and classes
 * from v3 bootstrap to v4 bootstrap, the currently being used in this application.
 */
export {
  datePickerTemplate,
  datePickerDayTemplate,
  datePickerMonthTemplate,
  datePickerYearTemplate,
};
